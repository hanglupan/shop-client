// search模块的小仓库

//state:仓库存储数据的地方
const state={

};
//mutations:修改state的唯一手段
const mutations={

};
// actions:提交mutation,处理异步
const actions={
    //可以提交mutation,但是不可以修改state
};
//getters:计算属性，多个组件可以同时获取一个数据
const getters={};

//对外暴露Store类的一个实例
export default{
    state,
    mutations,
    actions,
    getters
};